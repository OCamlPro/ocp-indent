module M = struct
  
  let f =
    
end

let g =
  
  fun x -> 3 + 4 *
               
module M =
  let rec unwind f path = match path with
    | { kind } :: _ when f kind -> path
    | { kind = KAttrId _ } :: { kind } :: _ when f kind -> path
      
